# This file tells Render that infrastructure is defined in render.yaml
# When importing a service, Render should automatically detect this file
# and use the infrastructure configuration instead of auto-detection
